module Opt where 



import Analysis
import Mat
import Action
import OR 
import Edge





--eNodeMat :: Matrix (OR Action) -> Matrix (Edge Int (OR Action))
--eNodeMat a@(M n _ _ _ _ _) = matrix n n (\(i,j) -> (i, a!(i,j) , j)) 


-- accesibleNodes_1 :: Semiring a => Matrix a -> Int -> [Int] 


-- accesibleNodes :: Int -> [Int] 


-- #TODO ?? accessible == inverse_star

 -- chokuwa bunkai matrix decomposition     
