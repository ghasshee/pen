contract Counter(
    uint256 _counter
    ){

    method(void init()){
        uint256 counter = 0;
        return then become Counter(counter); 

    }

    method(void inc()){
        uint256 counter = _counter + 1;
        return then become Counter(counter);
    }
}

